# Test Data Specification

Generate reproducible fictional reports demonstrating:

- abnormal current value;
- large change inside the interval;
- repeated rising and falling tendencies;
- stable value;
- no prior comparison;
- changed reference interval;
- ALT/ALAT/GPT alias;
- overlapping profile membership;
- missing profile member;
- ambiguous unit;
- duplicate file;
- patient identity mismatch.

Produce JSON, CSV, and selectable-text PDF from one source fixture, plus machine-readable expected results.
