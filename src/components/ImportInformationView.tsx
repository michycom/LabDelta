import { CheckCircle2, FileLock2, FileUp, ShieldCheck, UserRound } from "lucide-react";
import { useI18n } from "../i18n";
import type { PatientListItem } from "../types";
import { CollapsiblePanel } from "./CollapsiblePanel";
export function ImportInformationView({ patient }: { patient: PatientListItem | null }) { const { t } = useI18n(); return <section className="information-view import-information-view"><CollapsiblePanel demoTarget="import-information" storageKey="import" subtitle={t("importBoundary")} title={t("import")}><div className="import-information-grid"><div className="disabled-dropzone" aria-disabled="true"><FileUp size={30} /><strong>{t("manualDisabled")}</strong><span>{t("noDialog")}</span><button disabled type="button">{t("chooseFile")}</button></div><section className="import-information-card"><FileLock2 size={20} /><div><strong>{t("fixtureArtifacts")}</strong><p>JSON · CSV · selectable-text PDF</p><small>{t("artifactHelp")}</small></div></section><section className="import-information-card security"><ShieldCheck size={20} /><div><strong>{t("safetyBoundary")}</strong><ul><li><CheckCircle2 size={14} /> {t("manifestOnly")}</li><li><CheckCircle2 size={14} /> {t("checksums")}</li><li><CheckCircle2 size={14} /> {t("noRealData")}</li></ul></div></section><section className="import-information-card"><UserRound size={20} /><div><strong>{t("patientAssignment")}</strong><p>{patient ? `${patient.displayName} · ${patient.id}` : t("noSyntheticSelected")}</p><small>{t("assignmentHelp")}</small></div></section></div><p className="import-disabled-statement">{t("importDisabled")}</p></CollapsiblePanel></section>; }
